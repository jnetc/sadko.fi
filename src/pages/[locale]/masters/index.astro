---
// Global Styles
import '@Styles/activity_page.css';
// Content
import query from '@Queries/page_masters';
// Types
import type { IActivityiesPage } from '@Types';
// Layouts
import Layout from '@Layouts/Layout.astro';
import Article from '@Components/activity-pages/Article.astro';
// Helpers
import { singleWithLinks } from '@Lib/prismiccms';

const lang = Astro.currentLocale as 'ru' | 'fi';

const response = await singleWithLinks(query, 'page_masters', 'masters', lang);

// LOCAL DATA!!!
// Remove CACHE folder to get updated data!!!
// import { SingleWithLinkLocaleData } from '@Lib/localdata';
// const response = await SingleWithLinkLocaleData(query, 'page_masters', 'masters', lang);
// Define Type IActivityiesPage
const data = response.data as IActivityiesPage;
// const content = data.articles.map(article => {
//   // data.map((article: IActivityArticle) => {
//   // });
//   console.log(article);
//   article.data.
//   return {, ...article } as IActivityArticle;
// });
---

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width" />
  <title>{data.seo_title}</title>
  <meta name="description" content={data.seo_description} />
</head>

<Layout>
  <main class="grid activities main">
    <div class="page__header">
      <h1>{data.title}</h1>
      {data.description && <p>{data.description}</p>}
    </div>
    {
      data.articles.map(i => {
        return <Article article={i.article} />;
      })
    }
  </main>
</Layout>
